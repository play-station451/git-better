<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Fira Code', monospace;
        }

        .input-field {
            background-color: #f3f4f6;
            border-color: #d1d5db;
            color: #374151;
        }

        .input-field:focus {
            border-color: #2563eb;
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.3);
        }
    </style>
</head>

<body class="bg-gray-900 h-screen flex items-center justify-center px-4">
    <div class="max-w-md w-full bg-gray-800 rounded-2xl shadow-xl p-8 space-y-6">
        <h1 class="text-3xl font-bold text-center text-white">Login</h1>

        <div class="space-y-4">
            <% if (locals.error) { %>
                <div class="p-3 text-sm text-red-600 bg-red-50 border border-red-200 rounded-lg">
                    <%= error %>
                </div>
            <% } %>

            <form action="/login" method="POST" class="space-y-5">
                <div class="space-y-2">
                    <label class="block text-sm font-medium text-gray-300">Username</label>
                    <input type="text" name="username" required
                        class="input-field w-full px-4 py-2.5 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-shadow text-gray-900 bg-gray-700 text-white">
                </div>

                <div class="space-y-2">
                    <label class="block text-sm font-medium text-gray-300">Password</label>
                    <input type="password" name="password" required
                        class="input-field w-full px-4 py-2.5 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-shadow text-gray-900 bg-gray-700 text-white">
                </div>

                <div class="space-y-2">
                    <div class="flex flex-col space-y-2">
                      <img src="/captcha" alt="CAPTCHA"
                        class="mx-auto border border-gray-700 p-2 rounded-lg bg-gray-700">
                      <input type="text" name="captcha" required
                        class="input-field w-full px-4 py-2.5 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-shadow text-gray-900 bg-gray-700 text-white">
                    </div>
                  </div>

                <button type="submit"
                    class="w-full bg-gradient-to-r from-blue-500 to-blue-600 text-white font-medium py-3 px-4 rounded-lg hover:opacity-95 transition transform hover:-translate-y-0.5 shadow-lg">
                    Login
                </button>
            </form>
        </div>

        <p class="text-center text-sm text-gray-400 mt-6">
            Don't have an account? <a href="/register" class="text-green-400 hover:underline font-medium">Register
                here</a>
        </p>
    </div>
</body>

</html>